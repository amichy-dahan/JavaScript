var firstPiece = { id: 101, name: 'Ofri' }

var seoncdPiece = { country: 'Israel'}



let { id, name } = firstPiece;
let { country } = seoncdPiece;

let combined = { id, name, country };

console.log(combined)